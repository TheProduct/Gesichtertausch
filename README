Gesichtertausch
==

to run this project you currently need to install 'opencv' and 'libdc1394' with macports.

   1. install macports [ http://www.macports.org/ ]
   2. run 'sudo port install opencv' in the terminal
   3. run 'sudo port install libdc1394 +universal' in the terminal

to successfully compile the project you additionally need to …

   1. adjust the absolute CINDER_PATH in XCode
